
```
自己的想法:
错误的地方:
1.slow.next 这里错了 -> 通过example2 知道此时的slow在4的时候 fast在null 这个时候就有bug -> 说明是fast的问题 —> 考虑把fast作为while loop的考虑因素


slow = head
fast = head

while slow.next and fast.next:
    slow = slow.next
    fast = fast.next.next

return slow




正确:
time:o(n)
space:o(1)

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
        
        slow = head
        fast = head
        
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        
        return slow




```
