
```
总结:

把list of list 转化为list of tuple
tuple_ = [tuple(i) for i in list] #这里是指对list里面任意一个元素(i) 把这个元素i转化为tuple的形式

answer[1] is a list of all players that have lost exactly one match #这里可以提示你用counter然后提取出 出现一次的频率的元素即可
answer[0] is a list of all players that have not lost any matches #这里提示如果元素出现在element[0]里面就不可以出现在element[1]里面 提示用hashset

```
