
```python
--* 复杂度:
时间: o(n^(k-1))
空间: 最差为o(n)



--*: 3um模版
  确保数组是排序好的 为了后面使用双指针 // nums.sort

  遍历第一个要考虑的数值 // for i in range(len(nums)-(k-1)) 这里表示第一个数值可以遍历的范围
    去重              //if i != 0 and nums[i] == nums[i-1]:
                     // continue
    
    剩下两个数值的时候考虑用two sum2 解决
    l,r = i+1, len(nums)-1
            while l < r:
                val = nums[r] + nums[l] + nums[i]
                if val == 0:
                    result.append([nums[r],nums[l],nums[i]])
                    
                    #这里开始对第二个数值去重
                    l = l+1
                    while nums[l] == nums[l-1] and l <r:
                        l = l+1

                elif val < 0:
                    l = l +1

                else:
                    r = r -1




--*: ksum模版(k>3)
  
  大于2个数值的时候:
    
    遍历第一个数值
      去重
      加入当前数组
      dfs
      回溯
    输出
  
  
  小于等于两个数值的时候:
    利用two sum2








```
