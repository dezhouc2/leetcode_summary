
```python
note: 没做到bug free
time:o(nlogn)
space:o(1)

class Solution:
    def canAttendMeetings(self, intervals: List[List[int]]) -> bool:
        
        #0............30
        #  5..10
        #       15..20
        
        
        #  2..4
        #       7......10
        
        #sort the intervals by [0]
        intervals.sort(key = lambda x:x[0])
        #result = [intervals[0]]
        
        for i in range(1,len(intervals)):
            if intervals[i-1][1] > intervals[i][0]:
                return False
        
        return True


```
