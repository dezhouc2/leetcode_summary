
```python
time:o(nlogn)
space:o(n)

注意:
这道题后面如果不给出更多的examples 是做不出来的 
怎么解决? 如果面试遇到的话 问能不能多给点examples 然后clarify 目前的interval要不和去除的interval合并？

class Solution:
    def eraseOverlapIntervals(self, intervals: List[List[int]]) -> int:
        
        intervals.sort(key = lambda x:x[0])
        count = 0
        
        # 1....2
        # 1.......3
        #      2..3
        #         3..4
        
        # 1...11
        # 1...............100
        #   2....12
        #     11.....22
        
        
        result = [intervals[0]]
        
        for i in range(1, len(intervals)):
            if result[-1][1] > intervals[i][0]:
                result[-1][1] = min(result[-1][1], intervals[i][1])
                count = count+1
            
            else:
                result.append(intervals[i])
        
        return count


```
