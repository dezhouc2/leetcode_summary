
```
思路:
题目提示了n//2的元素数量为多数的标准。
考虑要按照什么标准的情况下，才能用n//2找出多数? -> 排序即可

time:o(logn)
space:o(1) #因为nums.sort()在本地上sort不用额外的空间


class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        
        nums.sort()
        return nums[len(nums)//2]
            


```
